# RPI_Time_Tracker
* Uses an LCD screen for a RPI that tracks the time from when my favorite person (gf) clocks out of work
* It uses a button on a breadboard to strike through the times as the day goes on. 


